"""
PLACEHOLDER ONLY — NO CODE.

Purpose:
- Merge TRAININGDATA.csv and COMPETITIONDATA.csv into MASTER.csv.
- Normalize schema (case_id, image_id/slice, bbox coords, class).

Inputs (intended):
- --train_csv path/to/TRAININGDATA.csv
- --comp_csv  path/to/COMPETITIONDATA.csv
- --out_csv   path/to/MASTER.csv

Implementation notes to self:
- Use pandas to read/concat, drop duplicates.
- Validate column names and types.
"""
